	private static String reverseString(String toReverse) {

		int length = toReverse.length();
		char[] chars = new char[length];
		for (int i = length - 1; i >= 0; i--)
			chars[length - i - 1] = toReverse.charAt(i);
		return new String(chars);
	}
